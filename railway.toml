[railway]
  service = "video-extractor"
  language = "node"

[railway.variables]
  PORT = 3000

# 构建配置
[railway.build]
  builder = "NIXPACKS"

# 部署配置
[railway.deploy]
  startCommand = "npm start"
  healthCheckPath = "/api/health"
